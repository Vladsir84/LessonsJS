const inputElem = document.querySelector('.search__input');
const buttonElem = document.querySelector('.search__btn');

buttonElem.addEventListener('click', () => {
   console.log(inputElem.value), true }
);